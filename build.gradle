apply plugin: 'java'

repositories {
    mavenCentral()
}

configurations {
    agent
}


dependencies {
    compile 'org.testng:testng:6.9.10'
    compile 'org.seleniumhq.selenium:selenium-java:2.53.1'
    agent "org.aspectj:aspectjweaver:1.8.0"
    compile "ru.yandex.qatools.allure:allure-testng-adaptor:1.4.0.RC4"
}






task testStandartCheckEdusson(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-standartChecksEdussonOnly.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}


task testStandartCheckPostDeploy(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-standartChecksPostDeploy.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}
testStandartCheckPostDeploy.doFirst {
    jvmArgs "-javaagent:${configurations.agent.singleFile}"
}

task testStandartCheckNotEasyBiddingSitesGateway(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-StandartCheckNotEasyBiddingSitesGateway.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}
task testStandartCheckNotEasyBiddingSitesPayPal(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-StandartCheckNotEasyBiddingSitesPayPal.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}

task testStandartCheckOnlyEasyBiddingSites(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-StandartCheckOnlyEasyBiddingSites.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}
task testAuthorization(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-UserAutorizationProduction.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"

    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}
task testExample(type: Test) {
    useTestNG {
        suites 'src/test/resources/testng-exampleTests.xml'
    }
    if (project.hasProperty('browser')) {
        systemProperty 'browser', "${browser}"
    }
    if (project.hasProperty('target')) {
        systemProperty 'target', "${target}"
    }
    if (project.hasProperty('platform')) {
        systemProperty 'platform', "${platform}"
    }
    if (project.hasProperty('site')) {
        systemProperty 'site', "${site}"
    }
}
